
## Release check-list

* Set Release (later: for now, everything is Debug)

* Bump version 
    * CMakeLists.txt
    * bindings/lisp/faudio.asd
    * doc/Doxyfile

* `make modules bindings doc test`

* Write release notes in NOTES, copy to github form

* Commit
    * `git add` (changed files)
    * `git commit -m "vX.X.X"`
    * `git tag vX.X.X`
    * `git push --tags`

* Run `release.sh X.X.X`

* Go to <https://github.com/hanshoglund/faudio/releases/new> 
    * Copy release notes
    * Upload the generated zip/tarball

* Run `.release.sh --clean`

