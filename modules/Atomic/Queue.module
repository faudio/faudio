
/**
    @defgroup DoremirAtomicQueue Concurrency: Atomic queues
    @brief Atomic queue (FIFO) data structure
 */
module Doremir.Atomic.Queue
{
    type Queue = IntPtr;
    
    create : () -> Queue;
    destroy : (Queue) -> Void;
    
    read : (Queue) -> IntPtr;
    write : (Queue, IntPtr) -> IntPtr;
}

