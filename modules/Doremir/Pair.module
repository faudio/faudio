
/**
    Provides the pair data structure. 
 */
module Doremir.Pair
{
    // FIXME needed by some allocation routines
    type Type = Size;

    type Func = Intptr;
    type Pair = struct { fst : Intptr, snd : Intptr };
    
    map : (Func, Pair) -> Pair;
}