
/** 
    Provides in-memory input and output.
    @author     Hans Höglund
 */
module Doremir.Audio.Device.BufferDevice
{
    type BufferDevice = Intptr;
    
    create  : (Size, Type) -> BufferDevice;
    destroy : (BufferDevice) -> Void;
}