
/** 
    @addtogroup DoremirBuffer
    
    Low-level buffer data structure.
 */
module Doremir.Buffer
{
    import Doremir.Std;

    type Buffer = opaque;

    create  : (Size) -> Buffer;
    copy    : Buffer -> Buffer;
    resize  : (Size, Buffer) -> Buffer;
    destroy : Buffer -> Void;

    size : Buffer -> Size;  
    peek : (Buffer, Int) -> UInt8;
    poke : (Buffer, Int, UInt8) -> Void;
}