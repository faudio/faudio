
/** 
    @addtogroup DoremirAtomicQueue
    
    Lock-free queue (FIFO) data structure.
 */
module Doremir.Atomic.Queue
{
    import Doremir;
    
    type Queue = opaque;
    
    create : () -> Queue;
    destroy : Queue -> Void;
    
    read : Queue -> Ptr;
    write : (Queue, Ptr) -> Ptr;
}

