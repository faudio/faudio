
/**
    @addtogroup DoremirSignal
    
    Provides signals.
    
 */
module Doremir.Signal
{
    import Doremir;
    import Doremir.Type;
    import Doremir.Processor;
    
    type Signal = opaque;

    getType : Signal -> Type;

    apply  : (Processor, Signal) -> Signal;
    apply2 : (Processor, Signal, Signal) -> Signal;
    apply3 : (Processor, Signal, Signal, Signal) -> Signal;

}