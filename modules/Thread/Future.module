
/**
    @defgroup DoremirFuture Concurrency: Futures
 */
module Doremir.Future
{
    type Future = Intptr;
    type Value  = Intptr;

    // TODO obtain result from what?
    create  : () -> Future;
    destroy : (Future) -> Void;
    
    isDone : (Future) -> Bool;
    wait   : (Future) -> Void;
    get    : (Future) -> Value;    
}

