
/** 
    Provides sequence composition of audio processors.

    ### Associated types

    Given audio processors *f* and *g*, *f >>> g* has the following properties:

    Kind           | Type
    -------------- | --------------------------
    State          | `(state f, state g)`
    Argument       | `(argument f, argument g)`
    Result         | `(result f, result g)`
    Input          | `input f`
    Output         | `output g`

    
    @defgroup   DoremirAudioProcessorSequence Audio processors: Sequence
    @author     Hans Höglund
 */
module Doremir.Audio.Processor.Sequence
{
    
}