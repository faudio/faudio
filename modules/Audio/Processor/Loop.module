
/** 
    Provides recursive composition of audio processors.

    ### Associated types

    Kind           | Type
    -------------- | --------------------------
    State          | `c`
    Argument       | `()`
    Result         | `()`
    Input          | `a`
    Output         | `b`
    
    
    @author     Hans Höglund
 */
module Doremir.Audio.Processor.Loop
{
    
}