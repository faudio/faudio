
/** 
    Provides non-real-time audio input and output.

    @defgroup   DoremirAudioDeviceFile Devices: Sound files
    @author     Hans Höglund
 */
module Doremir.Audio.Device.File
{
    type Device = Intptr;

    create      : (FilePath, FilePath) -> Device;
    destroy     : (Device) -> Void;
    openStream  : (Device, Proc) -> Stream;
    closeStream : (Stream) -> Void;
}