
/** 
    Provides real-time audio input and output.

    @defgroup   DoremirAudioDeviceAudio Devices: Audio
    @author     Hans Höglund
 */
module Doremir.Audio.Device.Audio
{
    type Device = Intptr;
    type Session = Intptr;
    type Stream = Intptr;
    type DeviceList = List;

    beginSession : () -> Session;
    endSession   : (Session) -> Void;

    devices         : (Session) -> DeviceList;
    standardDevices : (Session) -> DevicePair;
    openStream      : (Device, Proc) -> Stream;
    closeStream     : (Stream) -> Void;
}