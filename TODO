
- Components_resolve should invoke cmake -D COMPONENTS_RESOLVED=1 to run the find... modules
- Check if we can make Fluidsynth compile against checked in Glib binaries instead of /usr/local
- Remove lib directory
- Remove external directory
- Remove external_libraries/unicode

Minor tasks, committed as 
    AE-10 Minor fixes
    
- Set visibility=hidden on OS X. See Apple documentation 'Dynamic Library
  Programming Topics'
- Update ScoreCleanerAudio/Resources/Info.plist, see Apple documentation
  'Framework Programming Guide'
- Logging functions on OS X must create dir ~/Library/Logs/ScoreCleaner if not present
- Auto-download packages not present